// 单元测试
import { getTileRowAndCol } from "../util/tile-transform";

// describe("getTileRowAndCol", () => {
//   it("should return the correct tile row and column for a given longitude and latitude", () => {
//     const [row, col] = getTileRowAndCol(0, 0, 0);
//     expect(row).toBe(0);
//     expect(col).toBe(0);
//   });

//   it("should return the correct tile row and column for a given longitude and latitude", () => {})
// })
// 不使用框架
console.log(getTileRowAndCol(0, 0, 0));
